// Package domain contém as structs e enums que modelam o domínio do ad server.
// São portadas das entities JPA do ad-commons (v1.4.4) — sem mudança de schema
// do MySQL compartilhado (ADR-006). Cada campo cita a coluna de origem em comentário.
//
// Portado de: br.com.intv.adserver.business.entity.* (ad-commons v1.4.4)
package domain
